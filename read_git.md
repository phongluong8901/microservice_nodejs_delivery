---
git checkout -b p1_setup
git add .
git commit -m "change: first"
git push origin -u p1_setup

---
git checkout -b p2_auth_service
git add .
git commit -m "change: first"
git push origin -u p2_auth_service

---
git checkout -b p3_frontend_setup
git add .
git commit -m "change: first"
git push origin -u p3_frontend_setup